<?php

declare(strict_types=1);

// [[ .Name ]] Web Server
$uri = parse_url($_SERVER['REQUEST_URI'], PHP_URL_PATH);

if ($uri === '/') {
    echo "<h1>Hello from [[ .Name ]] PHP Web!</h1>";
} else {
    http_response_code(404);
    echo "404 Not Found";
}
