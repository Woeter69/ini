<?php

declare(strict_types=1);

// [[ .Name ]] JSON API
header('Content-Type: application/json');

echo json_encode([
    'message' => 'Hello from [[ .Name ]] PHP API!',
    'timestamp' => time(),
    'status' => 'success'
]);
