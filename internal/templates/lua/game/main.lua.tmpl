-- [[ .Name ]] LÖVE Game
function love.load()
    print("Welcome to [[ .Name ]] (Lua Game)!")
    msg = "Hello [[ .Name ]]!"
end

function love.update(dt)
    -- Update logic here
end

function love.draw()
    love.graphics.print(msg, 400, 300)
end
