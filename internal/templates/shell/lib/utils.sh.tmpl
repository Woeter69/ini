#!/usr/bin/env bash
# [[ .Name ]] — Utility functions
# Generated by ini

log_info() {
    echo "[INFO] $(date +'%Y-%m-%d %H:%M:%S') — $*"
}

log_error() {
    echo "[ERROR] $(date +'%Y-%m-%d %H:%M:%S') — $*" >&2
}

die() {
    log_error "$@"
    exit 1
}
