#include <cstdint>

// [[ .Name ]] — Embedded C++
// Modern C++ minimal main loop

extern "C" void system_init() {
    // Hardware initialization
}

int main() {
    system_init();
    
    while (true) {
        // Main loop
    }
    
    return 0;
}
