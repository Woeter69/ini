console.log("Welcome to [[ .Name ]] - Bun CLI Script");

const args = Bun.argv;
console.log("All arguments:", args);

if (args.length > 2) {
  console.log("Custom arguments provided:", args.slice(2));
} else {
  console.log("No custom arguments provided. Try: bun index.js hello world");
}

// Simple environment check
console.log("Running in environment:", process.env.NODE_ENV || "development");
console.log("Project name from INI:", "[[ .Name ]]");
