package main

import (
	"flag"
	"fmt"
)

func main() {
	name := flag.String("name", "World", "a name to say hello to")
	flag.Parse()

	fmt.Printf("Welcome to {{ .Name }} - CLI Application\n")
	fmt.Printf("Hello, %s!\n", *name)
}
