const std = @import("std");

// [[ .Name ]] — Zig Game Start
// Placeholder for a game loop using Raylib or similar

pub fn main() !void {
    std.debug.print("Game [[ .Name ]] starting...\n", .{});
    
    // Init window
    // Load assets
    
    while (true) {
        // Update
        // Draw
    }
}
