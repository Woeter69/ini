const std = @import("std");

// [[ .Name ]] — Embedded Zig
// Typical for Cortex-M or similar

pub fn panic(msg: []const u8, error_return_trace: ?*std.builtin.StackTrace, _: ?usize) noreturn {
    _ = msg;
    _ = error_return_trace;
    while (true) {}
}

export fn main() noreturn {
    // Hardware init
    while (true) {
        // Main loop
    }
}
